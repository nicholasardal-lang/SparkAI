"use client";
import {useState} from "react";
import {api} from "../auth-form";
export default function ForgotPassword(){const [email,setEmail]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);return <main className="auth-card"><a className="brand" href="/">✦ <span>Spark</span></a><h1>Reset your password.</h1><p>Enter your email and we’ll send a secure reset link if an account exists.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);try{await api("auth/forgot-password","POST",{email});setMessage("If that email is registered, a reset link is on its way.");}catch(e:any){setMessage(e.message);}finally{setBusy(false);}}}><label htmlFor="reset-email">Email address</label><input id="reset-email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><button className="button" disabled={busy}>{busy?"Sending…":"Send reset link"}</button></form>{message&&<p role="status">{message}</p>}<small><a href="/login">Back to login</a></small></main>}
